# Base URL of the API gateway / aggregated services. Read at server runtime only
# (web-client/src/lib/api/client.ts is "server-only" — never inlined into the browser bundle,
# deliberately not NEXT_PUBLIC_-prefixed so the same built image works across every deployment
# target with a different value set here).
API_BASE_URL=http://localhost:8080
