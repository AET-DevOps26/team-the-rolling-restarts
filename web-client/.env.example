# Public base URL of the API gateway / aggregated services.
# Used by the client at build/runtime. Variables prefixed with NEXT_PUBLIC_
# are inlined into the browser bundle.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
