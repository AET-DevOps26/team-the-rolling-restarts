---
project_root: /opt/rolling-restarts
compose_file: docker-compose.yaml
compose_prod_file: docker-compose.prod.yaml
compose_env_path: .env
app_service_name: rolling-restarts
health_check_retries: 60
health_check_delay: 20

registry: ghcr.io/aet-devops26/team-the-rolling-restarts
registry_user: ""
registry_token: ""
image_tag: latest

# Demo account seeded after deploy (see the "Seed demo user" tasks below) — a known login
# for demonstrations, not sensitive by design. Real (not placeholder) defaults on purpose:
# override in group_vars/all.yml only if you actually want different demo credentials, not
# because these need "replacing" first — matches the DEMO_USERNAME/DEMO_PASSWORD repo
# secrets used for the GitHub Actions-driven deploys.
demo_username: demo
demo_password: "Demo12345!"
