{%- set ns = [] -%}
{%- for key, value in app_env | dictsort -%}
{%- if key not in ['REGISTRY', 'IMAGE_TAG'] -%}
{%- set _ = ns.append(key ~ '=' ~ (value | string | regex_replace('[\r\n]', ''))) -%}
{%- endif -%}
{%- endfor %}
REGISTRY={{ registry }}
IMAGE_TAG={{ image_tag }}
{{ ns | join('\n') }}
